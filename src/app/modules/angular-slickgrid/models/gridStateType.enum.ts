export enum GridStateType {
  columns = 'columns',
  filter = 'filter',
  pagination = 'pagination',
  pinning = 'pinning',
  sorter = 'sorter',
  rowSelection = 'rowSelection',
  treeData = 'treeData',
}
